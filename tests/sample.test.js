const request = require('supertest');
// const { app } = require('./server/app.js');
// const { db } = require('./database/index.js');

// test('This is just a quick test', () => {
//   expect(1).toBe(1);
// })
describe('Sample Test', () => {
  it('should test that true === true', () => {
    expect(true).toBe(true);
  })
})